server:
  port: 7000

spring:
  application:
    name: gulimall-coupon
  profiles:
    active: prod
  cloud:
    nacos:
      discovery:
        server-addr: nacos-service.gulimall:8848
      config:
        server-addr: nacos-service.gulimall:8848
        file-extension: yml