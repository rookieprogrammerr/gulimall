server:
  port: 9000

spring:
  application:
    name: gulimall-order
  profiles:
    active: prod
  cloud:
    nacos:
      discovery:
        server-addr: nacos-service.gulimall:8848
      config:
        server-addr: nacos-service.gulimall:8848
        file-extension: yml