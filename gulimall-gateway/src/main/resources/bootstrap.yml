server:
  port: 88

spring:
  application:
    name: gulimall-gateway
  profiles:
    active: prod
  cloud:
    nacos:
      discovery:
        server-addr: nacos-service.gulimall:8848
      config:
        server-addr: nacos-service.gulimall:8848
        file-extension: yml