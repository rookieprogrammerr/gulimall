server:
  port: 8080 #11111

spring:
  application:
    name: gulimall-product
  profiles:
    active: prod
  cloud:
    nacos:
      discovery:
        server-addr: nacos-service.gulimall:8848
      config:
        server-addr: nacos-service.gulimall:8848
        file-extension: yml
